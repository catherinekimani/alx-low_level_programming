#include "main.h"
/**
* add - adds two integers and returns the result
* @first: int to be added to second
* @second: int to be added to first
* Return: sum of first and second
*/
int add(int first, int second)
{
	int result;

	result = first + second;
	return (result);
}
