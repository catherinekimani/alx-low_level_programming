#include "main.h"
/**
 * _abs - Prints the absolute value of a given integer
 * @n: the integer to be checked
 * Return: The absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
