#include <stdio.h>
/**
 * main - entry point
 * program that printsits name
 * @argc: ARGument count
 * @argv:ARGument Vector
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
