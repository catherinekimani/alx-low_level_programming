# include "main.h"
# include <stdio.h>
/**
 * _puts - function that prints a string,
 * followed by a new line, to stdout.
 * @str: string to be tested
 * Return: 0
 */
void _puts(char *str)
{
	puts(str); /* writing string to stdout*/
}
