# include "main.h"
# include <stdio.h>
#include <string.h>
/**
 *  _strlen -  function that returns the length of a string
 *  @s: Character to be checked
 *  Return: 0;
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
